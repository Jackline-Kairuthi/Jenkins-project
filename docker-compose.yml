version: "3.9"

services:
  backend:
    image: jkairuthi/test-backend:latest
    container_name: backend
    ports:
      - "5000:5000"

  frontend:
    image: jkairuthi/test-frontend:latest
    container_name: frontend
    ports:
      - "3000:3000"
